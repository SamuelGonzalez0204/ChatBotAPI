services:
  - type: web
    name: chatbot-flask
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: HUGGINGFACE_ACCESS_TOKEN
        value: 
